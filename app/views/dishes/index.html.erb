<h1>All Dishes</h1>

<%= button_to "Sort", sort_path, method: :post %>

<ul>
    <% @dishes.each do |dish| %>
        <li><%= link_to "#{dish.name} - #{dish.ingredients.size}", dish_path(dish) %></li>
    <% end %>
</ul>

<%= link_to "New Dish", new_dish_path %>
